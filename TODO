- Create SSH key entry for StudentInfo
- Insert SSH keys into SSH Key entry. This way we can move
the object(s) and not worry about keeping studentIDs or githubIDs
with the key -- keep all together.
- Create cmd line arugments. These flags/options include
            -f : Config file
            -U : Update SSH keys
            -A : Gets Keys, create accounts, update keys
            -i : Get the identity of the follwing name
- Create functions to create accounts
- Create functions to update SSH keys
